@model DSEDFinalProjectTripPlanner.DTO.TodoDTO

@{
    ViewData["Title"] = "Details";
}

<h2>@Html.DisplayFor(model => model.MyTodos.Title)</h2>

<div>
    <h4>Todo</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.MyTodos.Title)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.MyTodos.Title)
        </dd>
    </dl>
</div>

<div class="col-md-12">
    @* @{Html.RenderPartial("Create", new Item());}*@
    @Html.Partial("~/Views/Items/Create.cshtml", new Item())
</div>

<div>
    <a asp-action="Edit" asp-route-id="@Model.MyTodos.Id">Edit</a> |
    <a asp-action="Index">Back to List</a>
</div>
